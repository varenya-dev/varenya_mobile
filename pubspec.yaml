name: varenya_mobile
description: Varenya Flutter Application.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  firebase_core: ^1.3.0
  firebase_auth: ^2.0.0
  json_annotation: ^4.0.1

dev_dependencies:
  build_runner: ^2.0.6
  flutter_test:
    sdk: flutter
  json_serializable: ^4.1.4

flutter:
  uses-material-design: true
